@model IEnumerable<Rate_A_Cop.Models.Officer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BadgeNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OfficerName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BadgeNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OfficerName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OfficerID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OfficerID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OfficerID })
        </td>
    </tr>
}

</table>
